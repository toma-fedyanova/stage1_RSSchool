import { InfoTask } from '../types/type';
export 
const taskText: Record<string, InfoTask> = {
  '1': {
  title: 'Универсальный селектор',
  contecst: 'Он применяет стили ко всем элементам страницы и обозначается символом * (звёздочка)',
  task: 'Задание: выберите всех животных, приц на машине'},
}